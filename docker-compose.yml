services:
  db:
    image: postgres:15
    container_name: crm-pod-a-db-1
    environment:
      POSTGRES_USER: devuser
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: ryze_dev
    ports:
      - "5433:5432"  # host:container

